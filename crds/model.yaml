# Generated by tools/generate_crds.py from schemas/model-schema.json
# Do not edit by hand; edit the schema and regenerate.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: models.ai.agentbox.io
  labels:
    app.kubernetes.io/part-of: agentbox
    app.kubernetes.io/managed-by: ai-ctl
spec:
  group: ai.agentbox.io
  scope: Namespaced
  names:
    kind: Model
    listKind: ModelList
    singular: model
    plural: models
    shortNames:
    - mdl
    categories:
    - agentbox
  versions:
  - name: v1beta1
    served: true
    storage: true
    subresources:
      status: {}
      scale:
        specReplicasPath: .spec.replicas
        statusReplicasPath: .status.replicas
        labelSelectorPath: .status.selector
    schema:
      openAPIV3Schema:
        type: object
        description: 'Serving-plane model: registry entry, weights source, architecture and runtime capabilities.'
        required:
        - spec
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            type: object
            properties:
              modelName:
                type: string
                description: Human-readable model name, as callers refer to it.
              modelHub:
                enum:
                - huggingface
                - s3
                - gcs
                - azureBlob
                - custom
                description: Where the weights live.
                type: string
              hubModelId:
                type: string
                description: Model identifier within the hub, e.g. meta-llama/Meta-Llama-3-70B-Instruct.
              downloadApi:
                type: object
                required:
                - hubUrl
                - apiEndpoint
                - downloadMethod
                properties:
                  hubUrl:
                    type: string
                    format: uri
                  apiEndpoint:
                    type: string
                    format: uri
                  downloadMethod:
                    enum:
                    - huggingfaceHub
                    - http
                    - s3
                    - gcs
                    - custom
                    type: string
                  requiresAuth:
                    type: boolean
                    default: false
                  authTokenEnv:
                    type: string
                  filesToDownload:
                    type: array
                    items:
                      type: string
                description: How the platform fetches the weights, including auth.
              modelArchitecture:
                type: object
                required:
                - architectureType
                - baseArchitecture
                properties:
                  architectureType:
                    enum:
                    - transformerDecoder
                    - transformerEncoder
                    - moe
                    - rnn
                    - custom
                    type: string
                  baseArchitecture:
                    type: string
                  numLayers:
                    type: integer
                    minimum: 0
                  numHeads:
                    type: integer
                    minimum: 0
                  numKeyValueHeads:
                    type: integer
                    minimum: 0
                  hiddenSize:
                    type: integer
                    minimum: 0
                  intermediateSize:
                    type: integer
                    minimum: 0
                  vocabSize:
                    type: integer
                    minimum: 0
                  ropeTheta:
                    type: number
                    minimum: 0
                  attentionType:
                    type: string
                description: Architecture family and layer/head geometry.
              modelSize:
                type: object
                properties:
                  totalParams:
                    type: string
                  totalParamsExact:
                    type: integer
                    minimum: 0
                  diskSizeGb:
                    type: number
                    minimum: 0
                  quantizationMethod:
                    type: string
                  quantizationPrecision:
                    type: string
                  quantizationMemoryGb:
                    type: number
                    minimum: 0
                  quantizationQualityScore:
                    type: number
                    minimum: 0
                    maximum: 1
                description: Parameter count and on-disk footprint, used for placement.
              contextWindow:
                type: object
                properties:
                  maxContextLength:
                    type: integer
                    minimum: 1
                  trainingContextLength:
                    type: integer
                    minimum: 1
                  supportsExtendedContext:
                    type: boolean
                    default: false
                  ropeScaling:
                    x-kubernetes-preserve-unknown-fields: true
                description: Maximum context and output length the model supports.
              supportsStreaming:
                type: boolean
                default: true
                description: Whether the model can stream tokens.
              supportsFunctionCalling:
                type: boolean
                default: false
                description: Whether the model supports tool/function calling.
              modalities:
                type: object
                properties:
                  input:
                    type: array
                    items:
                      type: string
                  output:
                    type: array
                    items:
                      type: string
                description: Input and output modalities the model accepts.
              tags:
                type: array
                items:
                  type: string
                description: Free-form labels for discovery.
              replicas:
                type: integer
                minimum: 0
                maximum: 10000
                default: 1
                description: Serving replicas. Ignored while a ModelAutoScaler owns this model.
              serving:
                type: object
                description: How to serve this model in-cluster. Omit for a registry-only entry that records
                  what the model is without running anything.
                required:
                - image
                properties:
                  image:
                    type: string
                    pattern: ^[a-z0-9]([a-z0-9._\-/]*[a-z0-9])?(:[\w][\w.\-]{0,127})?(@sha256:[a-f0-9]{64})?$
                  entrypoint:
                    type: string
                  args:
                    type: array
                    items:
                      type: string
                    maxItems: 64
                  port:
                    type: integer
                    minimum: 1
                    maximum: 65535
                    default: 8000
                  env:
                    type: object
                    additionalProperties:
                      type: string
                  compute:
                    type: object
                    description: Compute request shared by workload-backed CRDs
                    properties:
                      cpu:
                        type: object
                        properties:
                          cores:
                            type: number
                            minimum: 0
                            maximum: 1024
                          threads:
                            type: integer
                            minimum: 0
                          memoryMb:
                            type: integer
                            minimum: 16
                      gpu:
                        type: object
                        properties:
                          enabled:
                            type: boolean
                            default: false
                          type:
                            type: string
                          count:
                            type: integer
                            minimum: 0
                          memoryMb:
                            type: integer
                            minimum: 0
                      storage:
                        type: object
                        properties:
                          persist:
                            type: boolean
                            default: false
                          sizeInGb:
                            type: integer
                            minimum: 0
                          iops:
                            type: integer
                            minimum: 0
                  nodeSelector:
                    type: object
                    additionalProperties:
                      type: string
                    description: Node labels the serving pods must land on, e.g. a GPU node group.
                  health:
                    type: object
                    properties:
                      type:
                        enum:
                        - http
                        - tcp
                        - exec
                        default: http
                        type: string
                      path:
                        type: string
                        default: /healthz
                      port:
                        type: integer
                        minimum: 1
                        maximum: 65535
                      command:
                        type: array
                        items:
                          type: string
                      initialDelaySeconds:
                        type: integer
                        minimum: 0
                        default: 10
                      periodSeconds:
                        type: integer
                        minimum: 1
                        default: 30
                  files:
                    type: object
                    description: Files written into the container from a ConfigMap or Secret. The key
                      is the mount path.
                    additionalProperties:
                      type: object
                      required:
                      - name
                      properties:
                        name:
                          type: string
                          pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                          maxLength: 253
                          description: ConfigMap or Secret name in the same namespace.
                        kind:
                          enum:
                          - ConfigMap
                          - Secret
                          default: ConfigMap
                          type: string
            required:
            - modelName
            - modelHub
            - hubModelId
            x-kubernetes-validations:
            - rule: '!has(self.serving) || self.serving.image != '''''
              message: serving.image is required when serving is set
          status:
            type: object
            description: Standard status subresource shared by every AgentBox CRD
            properties:
              state:
                enum:
                - pending
                - active
                - degraded
                - suspended
                - completed
                - failed
                - inactive
                - unknown
                type: string
              message:
                type: string
              observedGeneration:
                type: integer
                minimum: 0
              conditions:
                type: array
                items:
                  type: object
                  required:
                  - type
                  - status
                  properties:
                    type:
                      type: string
                      minLength: 1
                    status:
                      enum:
                      - 'True'
                      - 'False'
                      - Unknown
                      type: string
                    reason:
                      type: string
                    message:
                      type: string
                    lastTransitionTime:
                      type: string
                      format: date-time
              lastTransitionTime:
                type: string
                format: date-time
              replicas:
                type: integer
                minimum: 0
                description: Total replicas observed. Read by the scale subresource.
              readyReplicas:
                type: integer
                minimum: 0
              selector:
                type: string
                description: Label selector in string form. Read by the scale subresource.
            x-kubernetes-preserve-unknown-fields: true
    additionalPrinterColumns:
    - name: Hub
      type: string
      jsonPath: .spec.modelHub
    - name: HubModel
      type: string
      jsonPath: .spec.hubModelId
    - name: State
      type: string
      jsonPath: .status.state
    - name: Age
      type: date
      jsonPath: .metadata.creationTimestamp
