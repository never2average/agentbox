# Install the AgentBox controller:  kubectl apply -k deploy/
# The CRDs must be installed first: kubectl apply -k crds/
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - rbac.yaml
  - controller.yaml
