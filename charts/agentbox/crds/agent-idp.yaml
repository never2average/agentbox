# Generated by tools/generate_crds.py from schemas/agent-idp-schema.json
# Do not edit by hand; edit the schema and regenerate.
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  name: agentidps.ai.agentbox.io
  labels:
    app.kubernetes.io/part-of: agentbox
    app.kubernetes.io/managed-by: ai-ctl
spec:
  group: ai.agentbox.io
  scope: Namespaced
  names:
    kind: AgentIdP
    listKind: AgentIdPList
    singular: agentidp
    plural: agentidps
    shortNames:
    - idp
    categories:
    - agentbox
  versions:
  - name: v1beta1
    served: true
    storage: true
    subresources:
      status: {}
    schema:
      openAPIV3Schema:
        type: object
        description: 'Serving-plane agent identity provider: workload identities, policy groups and default
          behavior.'
        required:
        - spec
        properties:
          apiVersion:
            type: string
          kind:
            type: string
          metadata:
            type: object
          spec:
            type: object
            properties:
              name:
                type: string
                description: Human-readable name for this identity provider.
              description:
                type: string
                description: What this identity provider covers.
              status:
                enum:
                - active
                - inactive
                description: Whether this identity provider is in force.
                type: string
              identity:
                type: array
                items:
                  type: object
                  required:
                  - type
                  properties:
                    type:
                      enum:
                      - user
                      - role
                      - service
                      - tenant
                      - agent
                      type: string
                    ids:
                      type: array
                      items:
                        type: string
                    roles:
                      type: array
                      items:
                        type: string
                    match:
                      type: array
                      items:
                        type: object
                        required:
                        - attribute
                        - op
                        properties:
                          attribute:
                            type: string
                          op:
                            enum:
                            - gt
                            - gte
                            - lt
                            - lte
                            - eq
                            - neq
                            - in
                            - notIn
                            - matches
                            - exists
                            type: string
                          value:
                            x-kubernetes-preserve-unknown-fields: true
                    tags:
                      type: array
                      items:
                        type: string
                    annotations:
                      type: object
                      additionalProperties:
                        type: string
                      description: Annotations projected onto the ServiceAccount, e.g. an IRSA role ARN.
                description: 'Identity matchers: which roles, service accounts or tags this applies to.'
              policyGroups:
                type: array
                items:
                  type: object
                  required:
                  - mode
                  - priority
                  - guardrails
                  properties:
                    name:
                      type: string
                    mode:
                      enum:
                      - enforce
                      - monitor
                      type: string
                    priority:
                      type: integer
                      minimum: 0
                      default: 10
                    conflictResolution:
                      enum:
                      - denyOverrides
                      - permitOverrides
                      - firstMatch
                      type: string
                    guardrails:
                      type: array
                      description: Names of the guardrails in this group.
                      items:
                        type: string
                        pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                        maxLength: 253
                        description: RFC 1123 name, as used by metadata.name
                    appliesTo:
                      type: object
                      properties:
                        targetType:
                          enum:
                          - agent
                          - tool
                          - gateway
                          - channel
                          - hardware
                          - runtime
                          - model
                          type: string
                        targets:
                          type: array
                          description: Names this policy group applies to.
                          items:
                            type: string
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                            maxLength: 253
                            description: RFC 1123 name, as used by metadata.name
                        selector:
                          type: object
                          properties:
                            tags:
                              type: array
                              items:
                                type: string
                            attributesMatch:
                              type: array
                              items:
                                type: object
                                required:
                                - attribute
                                - op
                                properties:
                                  attribute:
                                    type: string
                                  op:
                                    enum:
                                    - gt
                                    - gte
                                    - lt
                                    - lte
                                    - eq
                                    - neq
                                    - in
                                    - notIn
                                    - matches
                                    - exists
                                    type: string
                                  value:
                                    x-kubernetes-preserve-unknown-fields: true
                description: Ordered groups of guardrails, with conflict resolution between them.
              defaultBehavior:
                enum:
                - deny
                - allow
                - monitor
                description: 'What happens when no policy matches: allow or deny.'
                type: string
              inheritFrom:
                type: string
                description: Name of another AgentIdP whose policy groups are inherited.
            required:
            - status
          status:
            type: object
            description: Standard status subresource shared by every AgentBox CRD
            properties:
              state:
                enum:
                - pending
                - active
                - degraded
                - suspended
                - completed
                - failed
                - inactive
                - unknown
                type: string
              message:
                type: string
              observedGeneration:
                type: integer
                minimum: 0
              conditions:
                type: array
                items:
                  type: object
                  required:
                  - type
                  - status
                  properties:
                    type:
                      type: string
                      minLength: 1
                    status:
                      enum:
                      - 'True'
                      - 'False'
                      - Unknown
                      type: string
                    reason:
                      type: string
                    message:
                      type: string
                    lastTransitionTime:
                      type: string
                      format: date-time
              lastTransitionTime:
                type: string
                format: date-time
              replicas:
                type: integer
                minimum: 0
                description: Total replicas observed. Read by the scale subresource.
              readyReplicas:
                type: integer
                minimum: 0
              selector:
                type: string
                description: Label selector in string form. Read by the scale subresource.
            x-kubernetes-preserve-unknown-fields: true
    additionalPrinterColumns:
    - name: Status
      type: string
      jsonPath: .spec.status
    - name: Default
      type: string
      jsonPath: .spec.defaultBehavior
    - name: State
      type: string
      jsonPath: .status.state
    - name: Age
      type: date
      jsonPath: .metadata.creationTimestamp
